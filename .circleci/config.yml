version: 2.1

orbs:
  cit: chime/ci-tools@1

defaults: &defaults
  docker:
    - image: circleci/ruby:2.5

jobs:
  test:
    <<: *defaults
    steps:
      - cit/bundler-preamble
      - run: bundle exec rake test

workflows:
  version: 2
  build_test_publish:
    jobs:
      - test
      - cit/gem-publish:
          context: gem-publish
          requires:
            - test
          filters:
            branches:
              only:
                - master
            tags:
              only:
                - /^pre-.*/
