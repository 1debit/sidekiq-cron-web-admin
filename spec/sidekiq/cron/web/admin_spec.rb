RSpec.describe Sidekiq::Cron::Web::Admin do
  it "has a version number" do
    expect(Sidekiq::Cron::Web::Admin::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
